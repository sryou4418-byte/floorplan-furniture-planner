## 최신 로컬 상태 — v0.7.0 / 2026-09-11

- 수도는 파란 점, 전기는 노란 점, 전기 3상은 빨간 점으로 표시한다. 상단 종류 버튼 선택 후 도면 클릭/탭으로 추가한다. 가구 위에도 표시할 수 있고 점은 가구보다 위에 표시된다.
- 점 클릭/탭 또는 우클릭 → 해당 표시 삭제. 가구 조작 버튼 또는 Esc로 표시 모드를 종료한다. 스크롤·드래그·멀티터치로 점을 생성하지 않는다.
- 점 좌표는 mm로 호실별 보관하고 기존 .fplan 파일 저장/불러오기에 포함한다. 이전 파일은 설비 표시가 없는 것으로 읽는다. 점은 가구 수·충돌·점유율 계산에서 제외한다.
- 우클릭 → 가구 편집에서 모양을 한 줄로 분리하여 가로·세로 입력칸을 같은 줄, 같은 너비와 높이로 정렬했다.
- 기본 작업은 사용자 컴퓨터의 로컬 저장·실행·테스트다. GitHub push, 원격 커밋/PR, 클라우드 업로드와 배포는 사용자가 별도로 요청한 경우에만 수행한다. 아래 과거 자동 push/배포 지침보다 이 원칙을 우선한다.
- 검증: Python 44개, Node DOM/제스처 26개, ruff, JS 구문 검사 통과. 로컬 실제 브라우저에서 세 색 점 추가·호실 왕복·삭제·가구 편집 정렬과 1500×800mm 적용을 확인했다. 모바일 실기기 및 기존 B4 오류 해결은 검증/완료로 간주하지 않는다.
- 로컬 실행: PowerShell에서 `.\.venv\Scripts\python.exe -m streamlit run app.py --server.address 127.0.0.1 --server.headless true --browser.gatherUsageStats false` 후 http://127.0.0.1:8501 접속.
- 이번 변경은 로컬에만 저장했으며 원격 main 및 공개 앱을 변경하지 않았다.

# Codex 시작 지침

현재 개발 버전은 v0.6.0이다. 2026-09-10에 확정한 데스크톱 7개 항목(B1~B3, D1~D4)을 구현했다. 도면 밖 드롭·크기 편집·회전 시 가구를 복구 가능한 위치로 보정하고, 복사 번호와 이름별 집계를 정리했다. 데스크톱 간격 표시·정렬 스냅·가구 옆 빠른 편집·빈 도면 우클릭 샘플 추가를 제공한다. 상세 구현과 검증은 CHANGELOG 및 `docs/HANDOFF_USABILITY_NEXT_BUILD_2026-09-10.md` 최상단을 따른다.

모바일 B4 편집 적용 실패는 이번 범위에서 제외되어 미해결로 남아 있다. 기존 모바일 탭 생성·하단 편집·길게 누르기 삭제는 삭제하지 않았다. 실행 취소·잠금·일괄 배치·DB·출력도 계속 후순위다.

사용자의 이후 작업 방식: 데스크톱 작업은 로컬 Git 저장소에서 개발·커밋·push한다. 모바일에서 별도로 원격 업로드를 지시한 경우에만 클라우드 작업 환경에서 GitHub에 직접 반영한다. 두 경로 모두 작업 시작 전에 `git pull --ff-only` 또는 동등한 최신 main 확인을 하고 force push하지 않는다.

오늘 작업은 인수인계로 종료했다. 다음 개발 요청 전에는 빌드를 만들지 않는다. 빈 도면 우클릭 → '샘플 가구 추가'로 클릭 위치에 한 개 생성하고 선택하며, 가장자리는 안쪽으로 보정한다. 가구 위 우클릭은 기존 가구 메뉴, 도면 밖 앱 여백은 생성 대상에서 제외한다.

## 최신 범위 정정 — 2026-09-10 (문서만)

최신 확정 범위는 데스크톱 7개 항목(B1~B3, 간격 표시, 정렬 보조, 가구 옆 빠른 편집+더블클릭, 빈 도면 우클릭 샘플 추가)이다. 모바일 수정·UI 재구성은 다음 버전에서 제외하고 B4는 미해결로 보존한다. 기존 모바일 기능을 삭제하지 않는다. 실행 취소·잠금·일괄 배치·DB·출력은 후순위다.

상세 동작과 합격 기준은 `docs/HANDOFF_USABILITY_NEXT_BUILD_2026-09-10.md` 최상단을 따른다. 우클릭 → 가구 편집 또는 더블클릭으로 가구 옆 소형 창을 열고, 확인/Enter 즉시 반영하며 취소/Esc·바깥 클릭·드래그는 미확정 입력을 폐기한다. 앱 코드·버전은 변경하지 않았고 v0.5.0을 유지한다.

이 저장소는 성환고 호실 도면 가구 배치용 Python/Streamlit 앱이다. 미국증시위험 프로그램과 별개의 프로젝트다.

작업 전에 최신 추가 인수인계 `docs/HANDOFF_USABILITY_NEXT_BUILD_2026-09-10.md`를 먼저 읽는다. 사용 목적·사용성 우선순위와 v0.5.0 이후 실제 제보(B1~B4)가 과거 완료 기록보다 우선한다. 가구 DB 업로드·출력은 후순위다.

이어서 다음 문서를 순서대로 읽는다.

1. `docs/HANDOFF.md`: 최신 사용자 의도, 현재 상태, 다음 개발 요구사항과 합격 기준.
2. `docs/HANDOFF_DESKTOP_UI_2026-09-10.md`: 최신 데스크톱 UI 단순화 기준.
3. `docs/DEVELOPMENT.md`: 구조, 데이터 불변 조건, 배포 절차.
4. `CHANGELOG.md`: 실제 구현·검증 기록.

현재 개발 버전은 v0.5.0이다. 데스크톱 도구 재배치와 모바일 호실 선택·삭제·고정 축척 문제를 개선했다. 실제 검증 범위와 잔여 확인은 CHANGELOG 및 HANDOFF 최상단을 읽는다. 과거 요구사항을 새 미구현 작업으로 오인하지 않는다.

v0.5.0에서는 데스크톱의 가구 추가·편집·호실 치수를 왼쪽 사이드바로 옮기고 저장/불러오기를 본문 하단에 뒀다. X/Y 직접 입력은 제거하고 표시 치수는 정수화했지만 내부 부동소수점 좌표와 파일 호환성은 유지한다.

- 최신 사용자 지시가 과거 문서보다 우선한다.
- 공유 저장/DB/로그인, 별도 앱으로 전환, 새 도면 공개는 승인 없이 추가하지 않는다.
- `끌면 사라짐`은 **편집창이 닫힘**을 뜻한다. 가구를 삭제하거나 숨기라는 뜻이 아니다.
- 모바일 길게 누르기는 흔들림+× 삭제 모드다. 기존 v0.3 길게 누르기 메뉴와 혼동하지 않는다.
- 호실 왕복 배치 유지, 그룹 이동, mm 기반 치수, v1/v2 파일 호환을 보존한다.
- 버그를 능동적으로 점검하고 회귀 테스트를 추가하되, 확인하지 못한 테스트는 통과로 보고하지 않는다.
- 매 빌드 UI 버전·CHANGELOG·인수인계를 함께 갱신한다. main 최신 상태와 작업 트리를 확인하고 강제 push하지 않는다.
- 앱 코드 변경 없이 문서만 수정하는 경우 런타임 버전을 올리지 않는다.
