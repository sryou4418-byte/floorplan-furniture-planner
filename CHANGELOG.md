## 최신 로컬 상태 — v0.7.0 / 2026-09-11

- 수도는 파란 점, 전기는 노란 점, 전기 3상은 빨간 점으로 표시한다. 상단 종류 버튼 선택 후 도면 클릭/탭으로 추가한다. 가구 위에도 표시할 수 있고 점은 가구보다 위에 표시된다.
- 점 클릭/탭 또는 우클릭 → 해당 표시 삭제. 가구 조작 버튼 또는 Esc로 표시 모드를 종료한다. 스크롤·드래그·멀티터치로 점을 생성하지 않는다.
- 점 좌표는 mm로 호실별 보관하고 기존 .fplan 파일 저장/불러오기에 포함한다. 이전 파일은 설비 표시가 없는 것으로 읽는다. 점은 가구 수·충돌·점유율 계산에서 제외한다.
- 우클릭 → 가구 편집에서 모양을 한 줄로 분리하여 가로·세로 입력칸을 같은 줄, 같은 너비와 높이로 정렬했다.
- 기본 작업은 사용자 컴퓨터의 로컬 저장·실행·테스트다. GitHub push, 원격 커밋/PR, 클라우드 업로드와 배포는 사용자가 별도로 요청한 경우에만 수행한다. 아래 과거 자동 push/배포 지침보다 이 원칙을 우선한다.
- 검증: Python 44개, Node DOM/제스처 26개, ruff, JS 구문 검사 통과. 로컬 실제 브라우저에서 세 색 점 추가·호실 왕복·삭제·가구 편집 정렬과 1500×800mm 적용을 확인했다. 모바일 실기기 및 기존 B4 오류 해결은 검증/완료로 간주하지 않는다.
- 로컬 실행: PowerShell에서 `.\.venv\Scripts\python.exe -m streamlit run app.py --server.address 127.0.0.1 --server.headless true --browser.gatherUsageStats false` 후 http://127.0.0.1:8501 접속.
- 이번 변경은 로컬에만 저장했으며 원격 main 및 공개 앱을 변경하지 않았다.

# 변경 기록

## v0.8.0 — 2026-09-11

- 저장 파일이 작업한 모든 호실을 포함한다는 의미가 드러나도록 파일명을 `호실배치_YYMMDD.fplan`로 단순화했다.
- 현재 선택된 호실명과 저장 시각은 파일명에서 제외했다. 날짜는 한국 시간을 기준으로 한다.
- 오른쪽 가구 목록 아래에 500자 이내의 작은 호실 메모 입력칸과 저장 버튼을 추가했다. 메모는 호실별로 분리하고 `.fplan` 파일에 함께 저장한다.
- 메모 필드가 없는 기존 `.fplan` 파일은 빈 메모로 불러온다.

## v0.6.0 — 2026-09-10

- 도면 경계 밖 드롭을 서버와 작업판 양쪽에서 보정. 그룹은 상대 위치를 유지하고 방보다 큰 가구는 치수를 줄이지 않은 채 복구 가능한 기준 위치에 둠. 크기 편집과 회전 뒤 경계 이탈도 같은 원칙으로 처리.
- 복사 이름을 `이름 2`, `이름 3` 규칙으로 변경하고 삭제된 번호는 가장 작은 빈 번호부터 재사용. 기존 `복사 복사` 이름도 새 복사 시 기본 이름으로 정리.
- 오른쪽 가구 집계에서 양끝 공백과 말미 구분 번호를 제외하여 `책상`, `책상1`, `책상 2`, ` 책상3 `을 하나로 합산. 도면에 저장된 개별 이름은 보존.
- 데스크톱 선택·이동 시 가장 가까운 벽 또는 가구까지 가로·세로 간격을 정수 mm로 표시. 겹친 축은 양의 간격으로 표시하지 않음.
- 이동 중 벽·다른 가구의 가장자리·중심 정렬선과 스냅 추가. `정렬 보조` 버튼과 Alt 일시 해제를 제공하고 그룹 상대 위치·실제 mm 정밀도를 유지.
- 더블클릭 또는 `우클릭 → 가구 편집`으로 가구 옆 소형 편집창 제공. 확인/Enter 즉시 적용, 취소/Esc·바깥 클릭·드래그 시 미확정 입력 폐기, 잘못된 값은 창 안에 표시.
- 데스크톱 빈 도면 우클릭 메뉴에서 클릭 위치에 1200×600mm 샘플 가구 한 개를 생성·선택. 가장자리 보정, 가구 메뉴 구분, 도면 밖 제외, 중복 생성 방어 유지.
- 앱·작업판·컴포넌트 캐시 버전을 v0.6.0으로 일치시킴. 모바일 탭 생성·하단 편집·길게 누르기 삭제는 유지하고 B4 적용 오류는 후속 모바일 범위로 보존.
- 검증: Python 35개, Node DOM/제스처 17개, ruff, JavaScript 구문 검사, `git diff --check` 통과. 실기기 및 공개 배포 화면 확인은 별도 기록 필요.

오늘 작업은 인수인계로 종료했다. 다음 개발 요청 전에는 빌드를 만들지 않는다. 빈 도면 우클릭 → '샘플 가구 추가'로 클릭 위치에 한 개 생성하고 선택하며, 가장자리는 안쪽으로 보정한다. 가구 위 우클릭은 기존 가구 메뉴, 도면 밖 앱 여백은 생성 대상에서 제외한다.

## 최신 범위 정정 — 2026-09-10 (문서만)

최신 확정 범위는 데스크톱 7개 항목(B1~B3, 간격 표시, 정렬 보조, 가구 옆 빠른 편집+더블클릭, 빈 도면 우클릭 샘플 추가)이다. 모바일 수정·UI 재구성은 다음 버전에서 제외하고 B4는 미해결로 보존한다. 기존 모바일 기능을 삭제하지 않는다. 실행 취소·잠금·일괄 배치·DB·출력은 후순위다.

상세 동작과 합격 기준은 `docs/HANDOFF_USABILITY_NEXT_BUILD_2026-09-10.md` 최상단을 따른다. 우클릭 → 가구 편집 또는 더블클릭으로 가구 옆 소형 창을 열고, 확인/Enter 즉시 반영하며 취소/Esc·바깥 클릭·드래그는 미확정 입력을 폐기한다. 앱 코드·버전은 변경하지 않았고 v0.5.0을 유지한다.

## 2026-09-10 — 다음 사용성 빌드 인수인계 (문서만)

- 이사 전 실측 가구 배치 가능성 검토를 주목적으로 명확화. 이사업체 전달·출력과 실제 가구 DB 업로드는 후순위.
- 도면 밖 가구 유실, 복사 숫자 이름, 말미 구분 번호·양끝 공백을 무시한 집계, 모바일 편집 적용 실패를 미해결 요구로 기록.
- 실행 취소, 조작 안정화, 모바일 간단 메뉴, 간격·정렬 보조, 빠른 복사, 목록 선택·잠금 개선 후보와 검증 기준 정리.
- docs/HANDOFF_USABILITY_NEXT_BUILD_2026-09-10.md 신설, AGENTS/HANDOFF에서 우선 읽도록 연결.
- 앱 코드·런타임 버전 변경 없음. 기능 구현·신규 테스트·예약 개발 없음. v0.5.0 유지.

## v0.5.0 — 2026-09-10

- 데스크톱 가구 추가·선택 편집·호실 치수 도구를 왼쪽 사이드바로 이동하고 프로젝트 저장/불러오기를 본문 하단으로 이동.
- 오른쪽에 배치된 가구의 이름별 개수와 총 개수를 표시하는 작은 요약 패널 추가. 1366px 화면에서 제목이 줄바꿈되지 않도록 폭과 글자 크기를 조정.
- 사용자용 X/Y 입력 제거. 치수·점유율은 정수로 표시하되 내부 좌표·기하 계산과 기존 파일 정밀도는 유지.
- 자체 호실 팝업을 모바일 기본 선택창으로 교체하여 한 번에 전환되고 사이드바 위에 목록이 잔류하지 않도록 수정.
- 모바일 가구 편집을 안전 영역을 고려한 화면 하단 고정 시트로 변경. 드래그 시작 시 기존처럼 미적용 편집창만 닫힘.
- 터치 이동 임계값을 14px로 분리해 작은 손떨림이 길게 누르기를 드래그로 바꾸는 현상을 완화.
- 길게 누른 뒤 × 삭제를 pointer down/up으로 처리해 터치 삭제 실패와 뒤 도면 이벤트 전파를 수정.
- 도면 맞춤 기준을 작업판에 보존하고 가구 재렌더·모바일 브라우저 높이 변화에는 재계산하지 않아 축척 변동을 방지.
- SVG 포커스 외곽선과 모바일 탭 강조를 제거해 도면 좌우 검은색·파란색 선 발생 경로를 차단.
- 검증: Node 제스처/DOM 테스트 13개, Python 구문 컴파일, JavaScript 구문 검사, git diff 검사 통과. 현재 작업 환경에 pytest/ruff가 없어 Python 30개 테스트와 ruff 재실행은 미실시.
- 공개 Chrome 검증: v0.5.0 로드, 데스크톱 좌측 도구/우측 요약/하단 저장 배치, native 호실 즉시 전환, 가구 추가 후 이름별 개수, 컴퓨터실 왕복 후 보건실 가구 복원 확인.
- 공개 Chrome에서 가구 추가 전후 SVG가 `257.587×655.2px`, viewBox가 `3440×8750`으로 동일해 재렌더 축척 유지 확인. X/Y 입력 미노출 확인.
- iPhone Safari/Android Chrome 실기기 터치·길게 누르기·× 삭제는 별도 확인 필요.

## v0.4.0 — 2026-09-10

- 모바일 빈 도면 탭으로 1200×600 샘플 생성. 작은 방은 비율 축소, 좌표는 탭 중심/방 안 보정.
- 가구 탭으로 도면 아래 이름·모양·mm 크기 편집. 적용으로 확정, 취소/드래그 시작으로 미적용 입력 폐기.
- 드래그 시 편집창만 닫힘. 길게 누르면 대상 가구 흔들림+44px × 삭제, 바깥 탭/완료로 종료.
- 제목·호실 선택을 작업판 레이아웃 안으로 통합하고 줄 높이 확보. 호실 팝업은 외부 터치/포커스와 사이드바 열기에서 닫힘.
- SVG 화면/제스처를 JS/CSS/순수 상태 분류 모듈로 분리. 모바일 빈 영역은 스크롤 허용, 가구는 포인터 캡처 이동.
- 멀티터치·취소·길게 누르기 뒤 잔여 탭, 중복 명령, 이전 위젯 값 덮어쓰기 방어. 가구 편집과 그룹 이동은 검증 후 일괄 반영.
- NaN/Infinity 좌표·치수를 모델/불러오기에서 거부.
- 검증: Python 30개, Node 제스처/DOM 테스트 11개, ruff 및 JS 구문 검사 통과. DOM 테스트는 합성 이벤트이며 실기기 테스트가 아님.
- 공개 Chrome 검증: 호실 목록→사이드바 열기 시 목록 닫힘, 이름/원형/900mm 편집, 호실 왕복 복원, 편집창 열린 채 드래그→창 닫힘 및 이동 좌표 반영 확인.
- 배포 점검 중 warm worker의 이전 actions 모듈 ImportError를 확인하여 의존 순서별 1회 재로드와 기존 세션 객체 마이그레이션 추가. 적용 완료 문구 잔류 제거, 데이터가 동일한 명령에도 재렌더되도록 revision 제공.
- iPhone Safari/Android 실기기 터치·주소창/키보드/핀치 검증은 별도 필요.

## 2026-09-09 — 다음 모바일 빌드 인수인계 (문서만)

- AGENTS.md와 docs/HANDOFF.md에 전체 맥락, 미구현 요구사항 M1~M6, 사용자 정정, 버그 점검 및 합격 기준 기록.
- 빈 도면 탭 생성, 가구 탭 이름/모양/치수 편집, 드래그 시작 시 편집창만 닫힘, 길게 누르기 흔들림+× 삭제를 다음 빌드로 확정.
- 사용자 스크린샷의 모바일 제목 잘림과 사이드바 위 호실 목록 잔류를 재현 항목으로 기록. 첨부 이미지는 추가 공개하지 않음.
- 오늘은 문서만 갱신. 런타임 코드·버전 변경 없음, 신규 기능 구현/검증/예약 개발 없음. 공개 버전 v0.3.0 유지.

## v0.3.0 — 2026-09-09

- 호실 목록 선택 즉시 전환, 첫 화면에 컴퓨터실 도면 표시.
- 상단 여백 축소, 전체 폭 작업판, 화면 높이 기반 맞춤, 작업판 내부 배율 도구 모음.
- 사용자 도면 업로드·진하기 조절 UI 제거. 기존 파일의 도면은 계속 읽을 수 있음.
- 모바일은 호실·도면 우선, 작업판 아래 접이식 가구 추가/편집 패널, 44px 터치 버튼.
- 가구 우클릭·550ms 길게 누르기로 이름 변경/회전/복사/삭제 메뉴.
- 이름 변경 확인/취소 및 Enter/Escape, 원형 회전 비활성화.
- 복사 시 방 안에서 겹치지 않는 후보 위치 탐색. 공간을 찾지 못하면 변경 없이 안내.
- 복사본은 원본 그룹에서 분리해 독립 이동. 필요한 경우 그룹 입력으로 다시 묶을 수 있음.
- 가구 명령을 planner/actions.py로 분리. 이벤트에 호실 키를 넣어 다른 호실의 지연 이벤트 무시.
- 상세 숫자 편집은 '변경 적용'으로 확정. 미확정 입력은 호실 전환 전에 적용 필요.
- 검증: pytest 19개 통과, ruff 및 JavaScript 구문 검사 통과.
- 공개 앱 검증: 우클릭 이름 변경·복사·90도 회전·삭제, 컴퓨터실→보건실→컴퓨터실 자동 전환 및 배치 복원, 110% 확대/화면 맞춤 확인.
- 배포 시 이전 작업판 모듈이 남는 현상 보완: 컴포넌트 등록 이름 버전 분리, 앱에서 작업판 버전 불일치 시 모듈 재로드.
- 모바일 실기기 검증은 별도 필요. 터치 메뉴 외에도 접이식 편집 패널로 모든 작업 가능.

## v0.2.0 — 2026-09-09

- 호실별 Workspace 분리: 호실 왕복 시 가구·좌표·그룹·모양·도면 유지.
- 여러 호실을 하나의 .fplan(v2 컨테이너)로 저장. 기존 단일 호실 파일 읽기 지원.
- 한국 시간 파일명: 층_호실명_YYYYMMDD_HHMMSS.fplan. 직접 설정은 층 생략.
- 저장 파일 생성 시점을 입력 처리 뒤로 이동해 최신 편집값 포함.
- 가구 외곽선 1px, 선택 2px, 방 경계 2px로 축소.
- 도면 배율 50~200%, 화면 맞춤 100%, 확대 시 작업판 내부 스크롤.
- 원형 가구 지름 입력 및 원/원·원/사각형 충돌, 실제 원 면적 계산.
- 가구 이름 아래 크기 표시, 긴 텍스트 크기 조정.
- 입력칸에서 Delete/Backspace를 눌러도 가구를 지우지 않도록 보호.
- 검증: pytest 16개 통과, ruff 통과. 공개 앱에서 원형 1개·이름/크기 2줄 표시,
  컴퓨터실→보건실→컴퓨터실 배치 복원 확인. 100% 작업판 높이 498px,
  200% 높이 994px(내부 스크롤), 실제 viewBox 6880×8750 유지 확인.
- 기존 파일을 불러온 뒤 '직접 설정' 공간으로 이동하는 경로도 회귀 테스트로 보강.
- 파일 복원은 자동 테스트로 검증. 이번 브라우저 검증에서 파일 업로드 왕복은 미실시.

### 제한 및 다음 작업

- 공유 저장 없음. 세션 종료/새로고침 전에 파일을 내려받아야 함.
- v2 파일은 이전 앱에서 열 수 없음. 새 앱은 v1/v2 모두 읽음.
- 호실 크기는 공칭치수. 문 열림·기둥·벽 내부 유효치수는 충돌 판정에 미포함.
- 면적 점유는 가구 면적 합계이며 겹친 부분을 중복 포함.
- 향후 실행 취소, 사용자가 만든 호실 추가, 세부 장애물 모델을 검토.

### 이전에 확정한 요구사항 — v0.3.0 구현 완료

- 도면 주변 여백 축소와 데스크톱 기본 화면에서의 도면 표시 영역 확대.
- 호실 목록 선택 즉시 전환, 별도 열기 버튼 제거.
- 사용자 도면 업로드·도면 진하기 기능 제거.
- 가구 우클릭/모바일 길게 누르기 메뉴: 이름 변경, 회전, 복사, 삭제.
- 모바일 전용 반응형 UI: 도면 우선 배치, 접을 수 있는 편집 패널·보조 메뉴, 터치 조작 크기 확보.
- 위 요구사항의 상세 기준과 검증 항목은 `docs/DEVELOPMENT.md`에 기록.

## v0.1 — 이전 빌드 요약

- 사각형 가구, 그룹 이동, 프로젝트 파일, PDF 2·3페이지 23개 호실.
- 500px 도면 맞춤, 드래그 좌표 입력창 동기화, Delete 키 삭제.
- 이름 아래 크기 표시는 이전 배포에서 누락되었으며 v0.2.0에서 반영.
